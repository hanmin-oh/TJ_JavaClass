작업파일 하나당 클래스 하나

JavaProject
|
Package
|
class


부모가 자식은 제어할 수 있지만 자식은 부모를 제어할 수 없다.



상속관계에서
|
부모의 메소드를 같은 메소드로 자식들이 다양하게 쓰면 일반 메소드
다형성 구현에서 자식들이 메소드의 이름은 같지만 다 다르게 써야하면 추상메소드


인터페이스는 추상 메소드만 있어야 한다. -- 일반 메소드를 사용할 수 없다.


상수를 초기화하는데 HP를 초기화를 다른 값으로 해야 하며 기본 생성자 안에
매게변수를 넣어야 한다.